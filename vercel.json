{
  "version": 2,
  "buildCommand": "cd deeptrace-frontend && npm run build",
  "outputDirectory": "deeptrace-frontend/dist",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/index.py"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
